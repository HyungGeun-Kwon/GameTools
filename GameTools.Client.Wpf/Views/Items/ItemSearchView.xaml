<UserControl x:Class="GameTools.Client.Wpf.Views.Items.ItemSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameTools.Client.Wpf.Views.Items"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <Label VerticalContentAlignment="Center" Content="Page number : "/>
        <TextBox Text="{Binding SearchPageNumber}" Width="50" VerticalAlignment="Center"/>
        <Label VerticalContentAlignment="Center" Content="Page size : " Margin="20 0 0 0 "/>
        <TextBox Text="{Binding SearchPageSize}" Width="50" VerticalAlignment="Center"/>
        <Label VerticalContentAlignment="Center" Content="Name filter : " Margin="20 0 0 0 "/>
        <TextBox Text="{Binding NameFilter}" Width="100" VerticalAlignment="Center"/>
        <Label VerticalContentAlignment="Center" Content="RarityId filter : " Margin="20 0 0 0 "/>
        <TextBox Text="{Binding RarityIdFilter}" Width="50" VerticalAlignment="Center"/>
        <Button Margin="20 10 0 10" Width="100" Content="Search" Command="{Binding GetItemsCommand}"/>
        <Button Margin="20 10 0 10" Width="100" Content="Cancel" Command="{Binding GetItemsCancelCommand}"/>
    </StackPanel>
</UserControl>
