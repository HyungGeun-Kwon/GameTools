<UserControl x:Class="GameTools.Client.Wpf.Views.Navigations.EntityNavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameTools.Client.Wpf.Views.Navigations"
             xmlns:viewModels="clr-namespace:GameTools.Client.Wpf.ViewModels.Navigations"
             d:DataContext="{d:DesignInstance Type=viewModels:EntityNavigationViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="240">
    <Grid>
        <ListBox ItemsSource="{Binding Entities}" SelectedItem="{Binding SelectedEntity, Mode=TwoWay}" FontSize="25" BorderThickness="1" BorderBrush="#DDD">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding EntityName}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
