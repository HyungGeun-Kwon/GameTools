<UserControl x:Class="GameTools.Client.Wpf.Views.Rarities.RaritySearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameTools.Client.Wpf.Views.Rarities"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button Margin="10" Width="100" Content="Search" Command="{Binding RaritySearchCommand}"/>
            <Button Margin="10" Width="100"  Content="Cancel" Command="{Binding RaritySearchCancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
