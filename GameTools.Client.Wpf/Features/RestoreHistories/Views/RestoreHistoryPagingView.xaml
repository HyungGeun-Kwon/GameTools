<UserControl x:Class="GameTools.Client.Wpf.Features.RestoreHistories.Views.RestoreHistoryPagingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:pageControls="clr-namespace:GameTools.Client.Wpf.Shared.Components.Pagination"
             xmlns:viewModels="clr-namespace:GameTools.Client.Wpf.Features.RestoreHistories.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:RestoreHistoryPagingViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d">
    <Grid>
        <pageControls:PagingBar
            PageState="{Binding PageState}"
            GoFirstPageCommand="{Binding GoFirstPageCommand}"
            GoPreviewPageCommand="{Binding GoPreviewPageCommand}"
            GoNextPageCommand="{Binding GoNextPageCommand}"
            GoLastPageCommand="{Binding GoLastPageCommand}"/>
    </Grid>
</UserControl>
