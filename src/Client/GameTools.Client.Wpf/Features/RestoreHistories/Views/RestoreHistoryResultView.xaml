<UserControl x:Class="GameTools.Client.Wpf.Features.RestoreHistories.Views.RestoreHistoryResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:localConverter="clr-namespace:GameTools.Client.Wpf.Shared.UI.Converters"
             xmlns:viewModels="clr-namespace:GameTools.Client.Wpf.Features.RestoreHistories.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:RestoreHistoryResultViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid
            ItemsSource="{Binding PageState.Results}"
            BorderThickness="1"
            AutoGenerateColumns="False"
            IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RestoreId}"/>
                <DataGridTextColumn Header="Is DryRun" Binding="{Binding DryRun}"/>
                <DataGridTextColumn Header="Actor" Binding="{Binding Actor}"/>
                <DataGridTextColumn Header="FiltersJson" Binding="{Binding FiltersJson}"/>
                <DataGridTextColumn Header="Target DateTime (UTC)" Binding="{Binding AsOfUtc}"/>
                <DataGridTextColumn Header="Started At (UTC)" Binding="{Binding StartedAtUtc}"/>
                <DataGridTextColumn Header="Ended At (UTC)" Binding="{Binding EndedAtUtc}"/>
                <DataGridTextColumn Header="AffectedCounts" Binding="{Binding AffectedCounts}"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
