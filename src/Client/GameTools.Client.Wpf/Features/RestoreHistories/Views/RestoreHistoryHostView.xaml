<UserControl x:Class="GameTools.Client.Wpf.Features.RestoreHistories.Views.RestoreHistoryHostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:region="clr-namespace:DotNetHelper.MsDiKit.RegionServices;assembly=DotNetHelper.MsDiKit"
             xmlns:name="clr-namespace:GameTools.Client.Wpf.Shared.Names"
             xmlns:overlayControls="clr-namespace:GameTools.Client.Wpf.Shared.Components.Overlays"
             xmlns:viewModels="clr-namespace:GameTools.Client.Wpf.Features.RestoreHistories.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:RestoreHistoryHostViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Auto">
            <ContentControl region:RegionServiceAttached.RegionName="{x:Static name:RegionNames.Restore_SearchRegion}"/>
        </ScrollViewer>
        <ContentControl Grid.Row="1" Margin="0 10 0 0" region:RegionServiceAttached.RegionName="{x:Static name:RegionNames.Restore_ResultRegion}"/>
        <ContentControl Grid.Row="2" region:RegionServiceAttached.RegionName="{x:Static name:RegionNames.Restore_PagingRegion}"/>

        <overlayControls:BusyOverlay
            Grid.RowSpan="100"
            IsActive="{Binding RestoreHistoryPageSearchState.BusyState.QueryBusy}"
            ShowCancel="True"
            IsHitTestVisible="True"
            CancelCommand="{Binding QueryCancelCommand}"/>
    </Grid>
</UserControl>
