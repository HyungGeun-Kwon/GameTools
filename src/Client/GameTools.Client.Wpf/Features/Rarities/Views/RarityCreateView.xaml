<UserControl x:Class="GameTools.Client.Wpf.Features.Rarities.Views.RarityCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameTools.Client.Wpf.Features.Rarities.Views"
             xmlns:viewModels="clr-namespace:GameTools.Client.Wpf.Features.Rarities.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:RarityCreateViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d">
    <StackPanel Margin="20" Width="320">
        <TextBlock Text="Grade"/>
        <TextBox Text="{Binding RarityCreateModel.Grade, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>

        <TextBlock Text="Color (#RRGGBB)" Margin="0 10 0 0"/>
        <TextBox Text="{Binding RarityCreateModel.ColorCode, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Content="Save" IsDefault="True" Command="{Binding SaveRarityCommand}" Margin="0 0 10 0"/>
            <Button Content="Cancel" IsCancel="True" Command="{Binding CancelRarityCreateCommand}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
